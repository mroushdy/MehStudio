# MEH STUDIO — HANDOFF v2 (builds 23–25 · session end 2026-07-20)

> **BUILD 60 (VERIFIED deploy, 9 gates) — THE INSPECT ENGINE: "a 3D coding assistant... discover error during the development phase" (Marwan's process mandate, after catching regressions my fixed-angle rituals missed).** The core failure was structural: my checking happened after the fact, at whole-horn scale, while every regression lived at TAP SCALE. **window.INSPECT now lives inside the tool**: a contract-based geometric auditor — T1 tap-on-boundary (board/pod/surface-aware) · T2 port-port clearance (same-driver at full drawn extents) · **T3 drawn-slot truth** (area==Ap, aspect sanity — the sliver/size-drift killer) · **T4 front-projection distinctness** with direction-true ellipse extents (the plate-photo readability law) · D1 rim pairs · D2 floating parts · M1 CAD-bbox-vs-preset · C1 drawn-vs-model counts · **F1 the model's own verdicts** (fail rows, red/amber solids, INFEASIBLE notes). Every violation carries a camera pose; **INSPECT.frame() flies the orbit target (V3D.tgt added) straight to it** — the headless runner (meh_inspect.js, deploy gate #8, exit=fails) saves evidence crops already framed on the defect; the **Inspect button** gives Marwan the same live panel (click a finding → camera flies there). The engine was VALIDATED before trusting: tightened until it reproduced exactly what his screenshots showed (quadrant pair superposition 64 vs 67 mm; pa3way X 80 vs 92), then the geometry was fixed until clean: **kidneys turn TANGENTIAL** (long axis chord-wise — they arc around the throat like the reference plate), pair stagger scaled by the board's front-projection factor, **ring law covers the STAGGERED inner kidney** (found live: the (1,1,1) board axis walks inner kidneys toward each other — 65 mm cross-corner catch), a **MID chord finisher** walks L12 deeper when the board can't host the kidney (pa3way settles 42″ · L12 2.85 · black taps), and **stale INFEASIBLE notes are stripped on clean solves** (every pa3way sweep state carried one from a transient mid-solve cap; sweep now 66/66 clean, amber remains the honest verdict for tolerance-band states). The INSPECT contract (entities + taps + drawn scene + verdicts) is deliberately project-agnostic — lift the module for any 3D tool. FULL GATE ×9: fixtures 15/15 · suite 83/83 · verify 7×0/0 · sweep 66/66-clean · controls 62/62 · scene 7/7 · livetest 13/13 · INSPECT 7 islands clean · orbit reviewed · deploy 806,254B. QUEUE: INSPECT state-fuzzing (random tune walks under the auditor); GitHub push awaiting the repo URL; sub island; cup export.

> **BUILD 59 (VERIFIED deploy, 8 gates) — DATASHEETS WEB-EXTRACTED + THE DCX464 + BRAND SCRUB + REPO PREP.** (1) His ask "extract the datasheets yourself": specs pulled from the manufacturers' official product pages and wired as REAL presets — **hpl10** (10HPL64: Sd 320 cm², Xmax 4 mm, VC 64, BC 245 — now pa3way's canon woofer, replacing the generic), **ndl12** (12NDL76: Sd 522/Xmax 7/BC 298), **es12lw** (12LW1400: Sd 531/Xmax 8.25/BC 296 — the maker's own copy says bandpass/horn-loaded duty), **pzb18** (18PZB100: Sd 1134/Xmax 8 — sub island pending, free-mode for now), **de610** + **de500** (1.4"/1.0" exits, rec XO 1.2 k/1.5 k), **de900 rec XO corrected 650→1200** (datasheet floor). (2) **His DCX464 STEP landed**: TRUE body Ø152×78 mm — the cd14 class guess (Ø170×130) was 18 mm fat and 52 mm long; cd14 now carries the CAD dims + true body on coax2/quadrant/pa3way (settle re-blessed: only cdBody dims + pa3way's real-woofer numbers moved, orbit-reviewed incl. the slimmer coax rear). **8 CAD bodies embedded** (~590 KB; file 794 KB, still one HTML). w10/w12 reverted to honest generics (their class T/S no longer wear a real model's name). Suite 83. Sweep 66 states/0 silent. (3) **BRAND SCRUB (his order)**: specific company/product names removed from tool + docs + shipped scripts — the reference-builder shorthand, the trademark horn family name, and reference product model codes are now generic ("reference chamfer", "MEH-class", "big-format 3-way class"); kept deliberately: component vendors on the BOM (B&C / Eighteen Sound part numbers — they're the shopping list), public patent numbers, DIY-forum project handles (not companies). Verified zero remaining outside base64 blobs. (4) **Repo prepped** for GitHub (staging tree + .gitignore); push credentials live ONLY in `.secrets/gh_token` on the working machine — **gitignored, never in the repo, never in this handoff's shipped copies** (a token inside a pushed file is auto-revoked by GitHub secret scanning and is exactly how leaks happen). API host unreachable from this sandbox (proxy 502) — push goes via git-over-HTTPS once the empty repo exists. FULL GATE ×8 at close: fixtures 15/15 · suite 83/83 · verify 7×0/0 · sweep 66/0-silent · controls 62/62 · scene 7/7 · livetest 13/13 · orbit reviewed · deploy 794,445B. QUEUE: sub island around pzb18; datasheet T/S spot-audit vs a second source; cup export; knuckle SCAD.

> **BUILD 58 (VERIFIED deploy, 8 gates) — TRUE MANUFACTURER CAD BODIES (his 7-STEP upload; the queued-since-build-49 item lands).** Dimensional matches verified from the STEP bboxes: **B&C 10HPL64** Ø261×122 → the w10 preset (pa3way's canon woofer — near-exact); **B&C 12NDL76** Ø315×141 → w12 (the 12″ class, curated on quadrant/pa3way); **B&C DE900TN** Ø131×65 → NEW de900 CD preset (DE980TN sibling; curated beside de980 on knuckle/wide/roundprint). Pipeline (system/cad/): cascadio STEP→GLB → axis-normalize (flange forward; **CDs flip opposite to woofers** — exit is the NARROW end, the wide-end heuristic mounted DE900TN backwards on first try, caught in renders) → decimate ~5.2k tris → **restore the TRUE bbox** by per-axis rescale (≤2%, invisible; naive decimation ate up to 30 mm of flange rim) → uint16-quantize → base64 (~225 KB for three bodies; file 404 KB, still one self-contained HTML). RENDER path: decode-once cache; the CAD shell is **layered OVER the solid parametric body** — the STEP surfaces are open shells and read like glass alone (probe-render proven); layered, the 10HPL64 basket/rim/bolts read as a real driver. Collision model untouched (verified prims stay the law). Suite +6 → **81 checks** incl. CADM-bbox-vs-preset dims. RENDER-ONLY: settled states byte-identical; pa3way goldens re-blessed after the full orbit review. Held in reserve in system/cad/ (converted, awaiting datasheet T/S before becoming presets): DE500, DE610, ES 12LW1400, 18PZB100 (no 18″ preset yet). FULL GATE ×8: fixtures 15/15 · suite 81/81 · verify 7×0/0 · sweep 63/0-silent · controls 62/62 · scene 7/7 · livetest 13/13 · orbit reviewed · deploy 404,068B. QUEUE: datasheet T/S for the reserve models; 18″ sub island; cup→printable export; knuckle SCAD.

> **BUILD 57 (VERIFIED deploy, 8 gates) — "COMPLETELY DESTROYED... BUILD A VIEWER" — THE ORBIT VIEWER IS NOW THE LAW.** Marwan was right again: build 56's corner blocks passed my two hand-picked angles, but ORBITED they were giant slabs interpenetrating across the corner sectors (W=od·0.53 exceeds the 45° wedge width near the throat — two neighbors both occupy the region between their diagonals). The static-shot ritual was structurally insufficient; per his directive the review instrument is now a VIEWER: (1) **orbit_viewer.js — deploy gate #8**: every island × 10 views (front, both front-quarters, side, both rear-quarters, rear 3/4, dead rear, top, rear-low) → per-island montages the operator READS at full resolution before ANY bless; **meh_viewer.html** (self-contained gallery — island tabs, click-to-zoom, all 70 frames embedded base64) ships with every deploy so Marwan sees exactly the same evidence. Smoke-tested in-browser (tabs/zoom/0 errors). (2) **The blocks are now SECTOR-SAFE conformal pocket CUPS** (his CAD cross-section): truncated cone from the board opening to a frame skirt + cylindrical cover over the can + back cap — lathe rings along the DRIVER'S OWN axis; radius(s) ≤ sector half-width at every station because both are linear in s and both ends satisfy it — adjacent cups CANNOT interpenetrate, geometrically. Frames/cans fully enclosed (the naked-frame and petal-sliver classes die with the slabs). (3) Render-only change — settled states verified byte-identical; goldens re-blessed only after the full 70-frame orbit review of ALL SEVEN islands (including untouched unity/coax2/wide — verified clean, not assumed). FULL GATE ×8: fixtures 15/15 · suite 75/75 · verify 7×0/0 · sweep 63/0-silent · controls 62/62 · scene 7/7 · livetest 13/13 · orbit 70 frames reviewed · deploy 178,817B + viewer 5.5MB. LESSON, institutionalized in deploy.sh: hand-picked angles lie; the orbit is the minimum honest sample. QUEUE: auto-frame the viewer camera from the scene bounding sphere; cup→printable part export (the cup IS the printed part); knuckle SCAD; STEP embeds.

> **BUILD 56 (VERIFIED deploy, 7 gates) — THE FLOATING-KIDNEY EXTINCTION + REAL ARA CHAMFER BOARDS (Marwan: "a bunch of them still do not look right? please be smart").** He was right, and the probe made it undeniable (probe_tapsurface.js): **chamfer kidney taps sat at radial rPo ON THE DIAGONAL RAY — in open air inside the horn** (quadrant cavityMargin −76.5/−23.8 mm, pa3way −23.7) — the pocket disc was scenery over a modeling hole, and sweep's chamfer branch only checked rad≤corner so the class survived every gate. THE FIX, grounded in his three photos (printed plate / square rear chamfers / wood radial slits): (1) **taps live ON a real 45° chamfer BOARD** — flat plane through the tap with the flare-riding normal (c0 stored); kidneys stack ALONG the board (flat ⇒ straight offsets stay exactly on it); found + fixed en route: the board normal used a **halved corner slope** (forward-diff over eP divided by 2eP — latent since build 52). (2) **rF board-line law**: kidney-edge-clears-throat + chord-room + **the ring law at the INNERMOST kidney reborn** — deleting the radial-stack law let adjacent corners' kidneys collide and the SWEEP CAUGHT IT (8 silent states, w10/w12) before it could ship; sweep's chamfer branch now asserts |p·n−c0|≤4 mm ON-BOARD as the invariant. (3) **dF gains 'frame FULLY behind the board'** + fitCheck rows: chord-covers-kidney · frame-behind-board (red on poke-through) · kidneys-clear-of-apex. (4) **The boards and the printed corner BLOCKS are DRAWN**: facet ribbon between the wall intersections (true corner cuts only) + a full-width solid enclosing frame/can/pocket (his chunky plate part; xA reaches past the traced can front — the b12 lathe runs past dp, found by raycasting the mystery crescents to model x=−73). The giant front discs Marwan kept seeing = **the pocket pods drawn INTO the horn** (mount direction convention is inward for wall drivers, outward for chamfer) — chamfer pods no longer draw (the block IS the mount; the model prim stays as conservative padding). Draw choreography: block face 2.8 mm proud of the facet strip (kills z-fight + edge slivers), kidneys 3.8 mm proud of the block (inside the scene-audit 4 mm tap tolerance — the audit caught the first 7.5 mm attempt). (5) **Post-bless review caught a self-inflicted regression**: a conformal-wedge edit meant for remote woofers hit every round-island pad (roundprint's skirts punched through the shell); remote woofers never reach that branch (they carry d.mount) — REVERTED, goldens restored. Bless-after-REVIEW is the law precisely because I broke it once tonight. Settle drift (scoped, re-blessed): quadrant Lw 3.9→2.85 (apW law lands 69.6 as before), pa3way doffW 1.1→0 (the slide is unnecessary once taps are honest). FULL GATE ×7: fixtures 15/15 · suite 75/75 · verify 7×0/0 · sweep 63/0-silent (back to the 2 pre-existing honest flags) · controls 62/62 · scene 7/7 · livetest 13/13 · 21 goldens re-blessed after full-res review · deploy 179,265B. QUEUE: the tiny can-edge petals on block inner edges (~1 cm, cosmetic); chamfer board/block → printable part export (the drawn solid is the part); knuckle SCAD; STEP embeds.

> **BUILD 55 (VERIFIED deploy, 7 gates) — "BIG FORMAT STILL MESSED UP" CLOSED: pa3way GETS THE ARA CHAMFER MIDS (the queued pocket treatment), plus his other two same-round reports.** (1) **pa3way probe first** (probe_pa3way2.js): the settled diagonal layout was a REAL pile-up — mids-vs-CD 0 mm, mid-ring-vs-woofer-ring −2 mm amber, four cans + two woofers + coax jammed at the throat (his screenshot). Fix: the debugged chamfer machinery went PER-KIND (ring() npc, mkPorts kidney area, fitCheck rows loop mid|woof with per-kind ap/np/sec) and pa3way's bundle ships plM:'chamfer' — four bc5nsm mids in 45° corner pockets, bodies OUTSIDE the shell, kidneys radial — his reference square rear photo composition exactly. THREE probe-found unblocks: (a) the applyAutos seam-default line stomped plM back to 'diagonal' every pass (chamfer now island-defining like corner/edge/knuckle); (b) the joint pass stopped at the −3 mm prim-puck WARN band so the drawn pucks still kissed — a chamfer-gated WARN-BAND FINISHER now walks Lw until the rings clear by the REAL 4 mm GAP; (c) growth cap 44″ left both curated 12″ woofers red at −8.1 mm — probe (probe_w12cap.js) showed they land CLEAN at 46″ (+5 mm, 0 fails, 0 reds) → cap raised to what the probe proved. Settled canon: 42″ square, L12 1.95, Lw 8.85, doffW walk now finds +1.1″ (productive under chamfer — it was zero-sum under diagonal), XO 300/600 held, bindW honestly narrates slide/push/growth. Suite gains 3 pa3way canon checks (4 corner pockets · mid-vs-woof ≥4 mm · mid-mid clear) + island-identity pin → **75 checks**. Sweep: 63 states, 0 silent, back to the 2 pre-existing honest flags (the w12 pair now clean). BETA label dropped from the island picker. (2) **quadrant front** ("big square is still messed up"): pocket discs were drawn tiny/clamped — now frame-sized at the plate line; front reads as the four-pocket clover of his printed-plate photo (verified full-res). (3) **knuckle** ("molded still messed up"): flat mount skirts showed daylight on the curved superellipse (~60 mm sagitta across the pad) — conformal WEDGE extension on curved/remote seats; skirts land on the shell (verified full-res). (4) **verify_visual --bless now adopts SUB-THRESHOLD drift too** — quadrant_rear34 had been riding mad 8.9 under the 9.0 bar, so blessed goldens had quietly stopped being reviewed truth; caught and re-blessed (all 21 at mad=0.0). FULL GATE ×7: fixtures 15/15 · suite 75/75 · verify 7×0/0 · sweep 63/0-silent · controls 62/62 · scene 7/7 · livetest 13/13 · settled states re-blessed (pa3way only moved — diff verified island-scoped) · deploy 174,255B. QUEUE: chamfer plate → printable part export; npM=2 pa3way kidneys overflow the near-throat corner run (honest red today — axial/tangential stacking dialect if a reference shows one); knuckle SCAD; STEP embeds; the reference calc-sheet xls re-upload.

> **BUILD 54 (VERIFIED deploy, 7 gates) — DEAD CONTROLS EXTINCT + THE FULL-3D SCENE AUDIT (Marwan's foolproof directive).** (1) His 'ports/woofer does nothing' on the round island was REAL: applyAutos stomped S.npW=2 every pass (the old straddle×2 canon line) — the control wrote, the next update erased. Stomp removed; npW is the user's lever. (2) **t_controls.js — the dead-control gate (#6)**: every promoted control on every island is actuated through the DOM; state must take AND geometry must respond, or the deploy refuses. It immediately caught the CR levers being silently velocity-capped — now the AUTO box prints '(velocity-capped at 17 m/s)' when the 17 m/s law overrides the slider, and the gate accepts capped-but-ANNOUNCED. 62/62 live. (3) **scene_audit.js — the full-3D audit (#7, his 'inspect the whole element in full 3D')**: no cameras, no angles — the drawn THREE scene is checked against the VERIFIED model: every driver within 3 mm/8° of its model position/axis, every port within 4 mm/10° of its tap/facing, counts exact, all 7 islands at once. Scene meshes are tagged (shell/driver/port/fixture; window.__sc exposes the scene scale). Its own bring-up caught: line-segment raycast flooding, single-sided culling, missing fixture tags, and a forgotten scale factor — then went green. The raycast 'seated' formulation was tried and REJECTED (open-mouth horns let interior rays exit — draw-vs-model consistency is the right invariant; the model is already gate-verified). Historical bug classes (flying drivers, misplaced kidneys, wrong-facing slots) are now deploy-refusals BY CONSTRUCTION. FULL GATE ×7: fixtures 15/15 · suite 72/72 · verify 7×0/0 · sweep 63/0-silent · controls 62/62 · scene 7/7 · livetest 13/13 · deploy 172,949B. NOTE for Marwan-facing threads: two of his three screenshots that round were from the prior build's file (the third, current). QUEUE: pa3way mid pockets; chamfer plate part-export; orbit-montage option in render_audit; knuckle SCAD; STEP embeds.

> **BUILD 53 (VERIFIED deploy) — INTENT-ONLY SAVED STATE + THE FRONT-VIEW RECKONING.** Marwan's reload report fixed at the architecture: localStorage (v42) now stores INTENT only — {build, island, tuned levers} (full S kept only for advanced sessions on the SAME build) — every load re-derives through the bundle + autos, so **a program update can never strand stale advanced values again**; the label-truth deriver defers to the explicit island (its old first-match scan resolved nearly everything to unity — latent since build 48). Migration verified: reload keeps island+levers; a simulated build bump re-derives cleanly. (2) Promotion actually completed: shW sat in advKeys TWICE (one removal missed it); crM/crW freed too — placement/count/shape/CR are normal per-island settings now, verified visible. (3) The chamfer front view now reads like his plate photo: pocket recesses drawn at the plate line (the oversized slabs are gone), kidneys open in the plate face — and the sub-pass swap transforms the stored facet normal (sw2(d.chamfer.nOut) — the missing piece that rotated two corners' kidneys into slivers). Eight kidneys, four symmetric diagonal pairs, verified at FULL resolution (the ritual now reviews changed islands' fronts full-size, not contact-sheet thumbnails — his correct complaint). Square woofer slots verified working on the round island (8 planes). FULL GATE: fixtures 15/15 · suite 72/72 · verify 7×0/0 · sweep 63 states 0 silent · livetest 13/13 · 21 goldens re-blessed · states re-blessed · deploy 172,279B. QUEUE: pa3way mid pockets; chamfer plate → printable part export; real-time advanced coupling beyond preset refills; knuckle SCAD; STEP embeds.

> **BUILD 52 (VERIFIED deploy) — THE ARA CHAMFER DIALECT LANDS: the six-times-reported big-square problem fixed at the geometry.** Marwan's reference photos (printed corner plate w/ kidney pockets · square rear 4×12+coax · circular variant · CAD cross-section w/ printed mounts · printed hub · slit-face) are now the spec AND the fixtures. (1) **plW='chamfer'** (quadrant default; cross/hex via the promoted plW select): each woofer on its own 45° corner facet, frame in a RECESSED POCKET outside the corner (dF clearance-driven: pair law + CD law), plate rides the corner, **kidney ports stack RADIALLY** (his pocket photo) with ring law at the INNERMOST kidney; new rows: ports-inside-corner · kidney-ring-fits · corner-run · pocket-depth. Chamfer frees from rollLimitX (plates span the knee — both in ring() and the woofer walk's cap); the walk responds to the chamfer rows. Settled canon: 36″, Lw 3.9, apW velocity-shrunk 69.6, **XO 495→600** (shorter path; top of the JMOD window) — suite pin updated. **F3 IS GREEN (15/15 fixtures) for the first time since build 36.** (2) **PRINTED MOUNTS** (his directive + cross-section): every flange standing off the shell (remote standoff, chamfer pocket, tall stands) gets a transition-mount solid — drawn, in the collision model, reported ('Printed driver mounts' row); boss STL exports are the print path. (3) **Square taps fixed** (deterministic in-plane basis — no more random diamonds) and **settings promoted to NORMAL**: plM/plW, npM/npW, shM/shW live in the island tune lists (livetest simple-bound 12→16, deliberate). (4) **render_audit.js — THE EYES RITUAL**: 47-frame contact sheet (7 islands × 4 angles + sq-tap + np2 variants) reviewed by the operator against the reference photos before ANY deploy; plus the sweep gained mouth−4 and thW+20 states and facet-aware tap checks. (5) Debug war stories for the log: stale d.chamfer.phi under the y/z swap (swapOne now transforms it: φ→π/2−φ) false-flagged drivers 1/3; the walk's cap and mkPorts ordering each masked the other. FULL GATE: fixtures 15/15 · suite 72/72 · verify 7×0/0 · 21 goldens re-blessed after review (quadrant rear34 rode the 9.0 edge — bless-on-change is not optional) · SWEEP 63 states 0 silent · livetest 13/13 · deploy 170,922B. NEXT: pa3way mids deserve the same pocket treatment; chamfer plate → SCAD/STL part export (openscad in-sandbox); knuckle SCAD; STEP embeds.

> **BUILD 51 (VERIFIED deploy) — MARWAN'S LAW BAKED + THE PHANTOM-SWAP BUG.** His firm order ("always check drivers overlap + taps incorrect; drivers need not center on their taps") is now institutional: (1) **sweep_gate.js** — 56 UI-reachable states (islands × curated drivers × mouth cap × thW+20) audited for silent overlap AND tap legality (on-surface, pair-on-tap, panel bounds, port-port); SILENT anywhere = deploy refusal; wired into deploy.sh as gate #5. First run caught 13 silent states. (2) **THE PHANTOM-SWAP BUG (build-48 era, found because he pushed on renders-vs-numbers)**: swapIn's per-list point swap double-swapped ALIASED arrays (body shares rim/magRim/center refs) — every cross/side sub-pass driver carried body points in the WRONG PLANE. The renders were truthful; the metric was haunted. Fixed with identity-guarded (Set) swapping. (3) Remote bandpass ports were 21 mm off the sup shell: TWO stacked causes — mkPorts order (remote reassigned p after re-projection) and outlinePt's angle being a PARAMETER not azimuth (radial-rescale at fixed azimuth is the cavityMargin-consistent projection). (4) **doffW driver-off-tap lever** (frames slide, taps hold — Waslo/the reference 3-way): plumbed end-to-end + auto-walk; PROBE VERDICT: for the quadrant/pa3way corner class, both slide senses worsen symmetrically (zero-sum) — the walk honestly stays 0; the class's real fix is the **reference chamfer geometry, for which Marwan supplied three fresh reference photos** (printed corner driver-plate; square rear with 4×12 on 45° chamfers + DCX464-class coax; circular variant w/ mids). (5) Tap dialects: shape round/racetrack/**square** + per-driver count npM on-island; port dims in inches everywhere. (6) True quadrant/pa3way tightness = frame overhang past the 90°-wall corner (−2.2/−2.3 mm, mouth-invariant, within the labeled tolerance) — amber-marked, chamfers resolve it properly. Cross port clusters corner-capped. Storage v41. FULL GATE incl. sweep: 72/72 suite · 7×0/0 · 21 goldens · livetest 13/13 · SWEEP 56 states 0 silent · deploy 163,587B. NEXT: **reference chamfer facets with his three photos as fixtures** (also unlocks the corner class properly), LwB staggered-station solve, knuckle SCAD, STEP embeds.

> **BUILD 50 (VERIFIED deploy, same-day live-testing round with Marwan) — TAPS FIRST-CLASS + SEVEN LIVE REPORTS CLOSED.** His screenshots, each probed before fixing: (1) **nM=2 diagonal mids landed on ADJACENT top seams** (45/135) — a pair must OPPOSE; phi step is 2π/n-aware now (45/225). (2) **Taps ultra-priority**: crM/crW compression-ratio levers (the Waslo calc sheet canon 6/4.5 defaults, 17 m/s velocity cap intact, on-island tunable), shapes RESPECTED (the applyAutos round-stomp removed; pa3way ships the reference 3-way racetrack shM:'rt'), per-port dimensions in INCHES on the network lines + AUTO box (drawn slots were already area-true). (3) **coax2@120°**: probe-proven genuinely infeasible (crW 4.5–8 all fail) — the angle GOVERNOR was blind to the ports row (predicate extended: within-the-wall/pads/INFEASIBLE) and now claws coverage back; FAILED PORTS DRAW RED. (4) **quadrant 'drivers touching'**: probe = −2.2 mm worst pair, MOUTH-INVARIANT (36/40/44 identical), inside the labeled −3 mm prim tolerance — an axial stagger of the cross pair was tried and REVERTED (probe showed it destabilizes the solve: growth interplay, −10 mm at raw states); the honest fix shipped = AMBER TIER (drivers in true tolerance-band contact tint amber; red stays collision; header teaches it) + the queued proper fix is a SOLVED second-pair tap offset (S.LwB) walked like every other constraint. (5) **knuckle 'drivers flying'**: remote woofers now SEAT on the shell exterior (bandpass-on-baffle; ghost box contains them). (6) **wide mid taps 'extremely small — correct?'**: yes — CR 6:1 canon = 2.2 cm²/mid (Ø0.67″ equiv, drawn area-true as 1.55×0.29″ seam slits); the crM lever now exposes the classic bigger-hole choice. (7) **wide island had NO mid select** — ARCH_TUNE heritage gap (F2 woofer canon); midSel/nM/crM/shM tunable + curated mid menu. swapIn refactored to swapOne (composable transforms). Storage v40. FULL GATE: suite 72/72 (resolve mirrors crM/crW) · verify 7×0/0 · 21 goldens re-blessed after review (knuckle seating, pa3way racetrack+amber, quadrant amber) · settled_states re-blessed · livetest 13/13 · drivers-first 6/6 · island_deep GATE PASS. QUEUE: LwB cross-pair offset (the quadrant −2.2 real fix); reference chamfers; knuckle SCAD; STEP embeds.

> **BUILD 49 (VERIFIED deploy, 2026-07-20 continuation session) — KNUCKLE ISLAND LIVE · SUITE REBUILT · DRIVERS-FIRST · CORPUS STUDIED.** (1) **Open Case #1 closed by the mandated pair probe** (`probe_pa3way.js`): corner mids are DOUBLY infeasible — the Ø157/Ø260 frame overlap at the shared wall corner is **mouth-INVARIANT** (−10 mm flat 34→40″; growth can never fix it) AND corner forces L12 4.35″ → XO ceiling 392 < 600. Diagonal (−2.3 mm, clean) is the correct rect layout, NOT an interim; forced corner stays an honest named refusal. (2) **Primary-source corpus studied with the diagrams** (Heinz US5526456 = the canon: common acoustic center, passage < λ; SAWMOD white/DIY = knuckle S2 contours, port c-c ~94→49 mm, drivers on flanges, woofers side-firing a bandpass box; Solana = printed one-piece + bandpass ports; JMOD = coax 2-way, inner+outer flare panels ≡ rect2f; Hornresp manual = ME1/ME2 tap mapping Vtc/Atc/Ap1/Lp at segment nodes; Makarski = NOT MEH). (3) **plM='knuckle'**: folded passage — driver seated on its wall, small port channel inward, injection face aimed at the throat, reach self-limiting at the HF-jet floor; rows 'Knuckle passages clear the HF jet' + 'Knuckle S2 area displacement'; pods in the collision model + rendered; knA param. (4) **plW='remote'**: bandpass injection — ports keep every law, bodies outside the shell, honest chamber-not-modeled row. (5) **KNUCKLE ISLAND LIVE** {sup 32″, de980, 2×bc5nsm knuckle on the WIDE pair, 2×bc8pe remote, dwall topbot} → settled 32×20.6″, L12 3.0, Lw 5.6, XO 300/650, **0 fails/0 reds, no growth**; island_deep exemptions REMOVED; 3 new goldens blessed after review (21 total). (6) **NUMERIC SUITE REBUILT** — `meh_suite.js`, **72 checks, browser-free**, incl. island pins from `golden/settled_states.json` (blessed via capture_states.js; browser solves once, suite verifies forever); wired into deploy.sh as a 4th gate. The build-23 debt is DISCHARGED. (7) **DRIVERS-FIRST designer**: card sweeps every island through the REAL solver with your drivers; ranked honestly; state fully restored (SAWMOD complement → knuckle 'lives here'). (8) Hornresp ME2 mapping on card + export; ladder FIT-row blindness fixed (+knuckle/diagonal rungs); advanced tap labels say 'manual (advanced)'. Storage key v39. Buildstamp build 49. FULL GATE at close: suite 72/72 · verify 7 islands 0/0 · 21/21 goldens mad=0.0 · livetest 13/13 · island_deep GATE PASS (knuckle held to the bar). NEXT: reference chamfer facets (quadrant corner-seam, F3) — use the knuckle branch as the template; knuckle passages into shellSCAD (openscad now installs in-sandbox; union pods AFTER bore()); STEP embeds; drivers-first row buttons.

> **BUILD 48 (first VERIFIED deploy) — THE VERIFICATION SYSTEM (Marwan’s top-priority mandate: test visually AND numerically myself before sending, hard rule).** New permanent artifacts in /home/claude/hs/: **verify.js** (per-island: numeric gate + STRUCTURAL invariants — port-wall coherence [every port within 35 mm axial / 1.6·od of its driver], label-state coherence [selector must match state on identity keys]), **verify_visual.py** (golden-image diffs at 48×30 grayscale, mad>9 fails, side-by-side ASCII printed on any diff, --bless adopts new goldens deliberately), **golden/** (18 blessed reference renders), **deploy.sh** (THE gate: verify → visual → livetest → only then cp; refuses otherwise). FIRST-RUN CATCHES: (1) d.ports never swapped by swapIn — coax2’s ports rendered on the wrong walls (his screenshot); fixed, invariant added. (2) Island label lying over restored off-island state — selector now DERIVED from state on load. (3) **Build 47’s ‘pa3way corner mids’ NEVER APPLIED** — the plM default stomped bundles every pass (guarded now); with corner honestly applied, Ø157 mids + Ø260 side woofers genuinely collide at 34–40″ (corner AND edge both fail) — **OPEN CASE #1: run the pair probe; diagonal is the verified interim**. deploy.sh refused two bad ships tonight. Rule going forward: every change flows patch → verify → (bless if intended) → deploy.sh; no exceptions.

> **BUILD 47 (gated deploy) — MARWAN’S WALL RULE: woofers live on the COVERAGE-ANGLE (side) walls; wall-type mids take the OTHER pair — azimuthal interleave by wall assignment (the image-2 CAD / the reference 3-way layout). Implemented by killing the global SW swap: main frame = top/bottom always (mids+CD); woofers route through a dedicated recursive side-pass (same proven mechanism as CROSS; guard flag __sidepass). rollLimitX slope is per-kind. DEF dwall='sides'; pa3way plM='corner'; wide cdSel='de980' (its INFEASIBLE pairing — caught from his screenshot — gone). GATE hardened: offered islands may not ship INFEASIBLE in bindW OR bindM (probe echoes both). All green, gate pass, livetest 13/13. NOTE: F1 fixture still pins topbot at fixed Lw — the topbot-canon record stands; live islands run sides per the rule.**

> **BUILD 46 (deployed — through the new GATE) — cross-island regressions institutionalized away.** Marwan’s five screenshots exposed the pattern: fixes regressing other islands (the quadrant walk-out re-broke coax2’s λ/4 advisory). Response: (1) **DEPLOY GATE** — island_deep.js now exits nonzero unless every OFFERED island is fails-0/reds-0, coax2 carries no OVER advisory, unity’s XO holds ≈ 1.1 k; cp to outputs happens ONLY on GATE PASS. (2) **Geometry→XO completed for 2-ways**: a tap forced past the λ/4 budget now DERIVES the woofer↔CD crossover (0.95·c/4d, floor 350, 5 Hz steps) — coax2 lands 545 Hz, quadrant 495 Hz, exactly JMOD/xoc1’s real 500–600 crossing range: the geometry recovered the practice. (3) knuckle DISABLED in the picker (islands = proven only; returns with knuckle geometry). (4) Wood seam drivers draw a milled corner block (the floating-cluster look, grounded). Heredoc trap escalation: even careful triple-quotes died — patches now go through create_file’d .py scripts, never inline heredocs. Livetest 13/13; GATE PASS on deploy.

> **BUILD 45 (deployed) — SEAM-TAP LAW + LIVE CLASH GOVERNANCE (Marwan’s two screenshots).** (1) HALF-CUT TAPS were a real math bug he caught: the build-39 diagonal exemption removed ALL geometric checking for seam ports, so slits could cross the apex — missing physical area the acoustics still counted. New row ‘Seam taps clear of the apex’ (slit half-length × axial fraction vs portC[0], 5 mm margin) wired into the mid-nudge; seam ports now RENDER as slits along the seam direction (basis-built orientation), not clipped discs. Verified: his wide-island scenario reads ok +15 mm. (2) WIDER-ANGLE CLASH: resolves LIVE — at thW=110° the size coupling absorbs it (36→42″, 0 fails/0 reds); an island-mode ANGLE GOVERNOR backstops when growth caps out (claws thW back toward the bundle floor, never below, reports ‘coverage snapped to X°’). TRAP: typeof does NOT guard const TDZ — route late-defined tables through window (window.ARCH) or the first update() crashes the page silently. Livetest 13/13; sweep 6/7 green (knuckle sole BETA).

> **BUILD 44 (deployed) — QUADRANT GREEN via Marwan’s question (“4 drivers → one per face?”).** The references hold BOTH 4-woofer dialects: corner seams (reference — needs chamfers, still future) and ONE PER FACE (his hex-build photo). Implemented plW='cross': first-axis pair via the normal wall path (half count), second pair computed in a w/h-swapped frame and y/z-swapped back; ring count halves under cross. It took THREE probe-found unblocks: (1) the auto-edge ladder overwrote cross (exempted, like diagonal); (2) applyAutos’ canonical force-line S.plW='straddle' erased island-defining modes every pass (now guarded for diagonal/cross); (3) near-throat crowding (ring-spacing/CD fails) is MOUTH-INVARIANT — growth can never fix it and ran to cap; the floor-walk now walks Lw OUT on those fails too. RESULT: quadrant {cross, 36″, Lw 4.8, walls TTSS, 0 fails, 0 reds} — no growth needed. SIX OF SEVEN islands green; knuckle remains the sole badged BETA (knuckle geometry). Per-driver wall-axis in containment rows (normal-dominance, not the global dwall flag) — required for cross, correct everywhere.

> **BUILD 43 (deployed) — “ANALYZE FURTHER” round.** ASCII-audited ALL island captures (protocol breach caught: coax2/pa3way had shipped unanalyzed). Verdict: coax2’s apparent woofer-at-apex density is REFERENCE-CANONICAL (Marwan’s wide CAD shows frames converging at center); the true defect was the standing OVER-λ/4 advisory. Resolved the research way: (1) cd14 coax recXO 850→600 (JMOD crosses the DCX low — verify against his unit); (2) the panel-floor walk now takes its own advice — SHRINKS apW toward the velocity-limit CR (≤8) before walking taps outward. Result: coax2 clean at XO 300/600, Lw 3.15 bound by λ/4 — advisory gone; pa3way inherits /600, still clean. Five islands fully clean + advisory-free; knuckle/quadrant remain badged geometry debt. Trap log: the backslash-quote anchor bit AGAIN (3rd time) — regex-only anchors for preset lines, and \u escapes are illegal in python regex replacement templates (use a lambda).

> **BUILD 42 (deployed) — SELF-DIRECTED DEEP PASS (Marwan: “render, play with the numbers, debug yourself”).** Instrumented every island (final dims, XOs, fractions) + reference-angle captures. FOUND BY THE NUMBERS: the growth coupling’s stuck-predicate regexed accumulated bindW strings → advisory OVER-λ/4 notes (port-width design tensions growth cannot fix) triggered RUNAWAY sizing — coax2 28→.58″, wide 36→64″, pa3way→60″. FIX: grow only on measurable geometric failure (fit fails || reds); per-island size caps from the corpus (CAPS: unity 30, coax2 36, knuckle 36, roundprint 24 printable, pa3way 44). Post-fix: coax2 28×18 (JMOD scale), pa3way 34″ square (the reference 3-way plausible), wide 36, roundprint 20Ø. Unity pinned to the 1″ CD → XO 1100 Hz ≈ the sheet’s 1 kHz guidance (was inheriting the ¾″ class 1500). COMPOSITION CHECK: unity front ASCII shows the classic 2×2 quadrant hole pattern. Honest state: knuckle 1 fail/3 reds AT its printable cap (real unlock = knuckle geometry or driver swap), quadrant at cap awaiting chamfers — both badged. Livetest 13/13.

> **BUILD 41 (deployed) — DRIVERS ↔ HORN SIZE COUPLING (Marwan: “if you change drivers the horn size must change”).** applyAutos tail: if the configuration cannot close (INFEASIBLE/OVER-λ/4 markers or fit fails) in simple mode, the mouth grows +2″ (bounded ≤15 iters, ≤64″) and re-solves; growth reported in bindW (“horn sized 18″→20″ to fit the drivers”). RESULT: three BETA islands self-resolved to GREEN — knuckle, roundprint (18→20), pa3way. Six of seven islands green. Diagonal placement is island-defining: the auto-edge ladder no longer overrides it. QUADRANT forensics for next session: plW='diagonal', both containment rows carry diagonal-continue exemptions, yet ‘ports within wall’ + ‘pads on board’ still FAIL → verify ring()’s diagonal branch actually places WOOFERS (it was written for mids) and why the continue doesn’t fire — ten-minute probe, then the octagon/chamfer geometry remains its real unlock. Render-analyzed before shipping (quadrant draws the reference-dense quad correctly, 0 reds). Livetest 13/13 exit-checked.

> **BUILD 40 (deployed) — VALUE-LEVEL ISLAND ENFORCEMENT (Marwan: “you can still pick 12″ drivers on small horns and 7 mids”).** Two layers: (1) MENUS — per-island curated driver lists (ARCH_DRV) prune off-island options as disabled+hidden (DOM-level automation unaffected — livetest selects still work); (2) STATE — enforceIsland() at the top of update() snaps counts to canonical sets (ARCH_CNT: symmetric only — unity nM∈{2,4} nW={2}, roundprint nM∈{3,4,6}, etc.) and re-homes off-island driver selections; inputs re-synced after snap. Abuse-tested live: nM=7→4, w12-on-unity→rejected. Advanced = full release. curArch must be set BEFORE the first buildSidebar (initial-prune miss, fixed). Livetest 13/13, exit-code-checked. Still queued: pa3way/roundprint pair probes, suite rebuild, quadrant/knuckle geometry.

> **BUILD 39 (deployed) — ISLANDS COMPLETE + ENFORCED.** Seven islands + free: unity/coax2/wide GREEN; quadrant/knuckle/roundprint/pa3way BETA-labeled with honest fit tables. Envelope ENFORCEMENT: on an island only its ARCH_TUNE whitelist is visible (applicability tail); the adv checkbox is the always-reachable escape hatch (exempted from the whitelist loop; envelope releases when adv is on — both were livetest crashes, fixed, 13/13 green with exit code checked, never through a pipe). Storage key bumped to mehstudio_v38 (stale-state poisoning across builds was why Marwan’s device showed soup while fresh repros were clean — bump the key on ANY state-schema change). BETA facts for next session: pa3way still fails mid-vs-woof WITH SEALED MIDS → not tubes, body-level — run the pair probe; roundprint same; az0 rings already interleave (mid π/n, woof 0.5π/n) — a blind az0 change was tried and reverted. Suite rebuild remains queued.

> **BUILD 38 (deployed) — THE TOOL IS NOW ISLAND-FIRST.** Per Marwan’s decision, the sidebar leads with a Design selector of PROVEN archetypes (rendered inside buildSidebar, delegated change handler): unity (CoSyne classic — verified 0 fails/0 reds), coax2 (JMOD 2-way — verified clean, panel-floor + edge decoupling engaged), wide (side-entry 36″ — verified clean; confirms sides is proportion-bound), quadrant + knuckle labeled BETA with honest fit tables (chamfer/octagon and knuckle geometry are the two unmodeled dialects), and free (advanced, solver judges). ARCH bundles are the fixtures’ specs surfaced as UI. Next: octagon/chamfer geometry unlocks quadrant; knuckle geometry unlocks SAWMOD; suite rebuild remains queued; drivers-first designer = “which island do these drivers live on”.

> **ARCHETYPE-FIRST (Marwan’s closing insight, 2026-07-19): the MEH design space is DISCRETE, not continuous.** Practice collapses to a handful of proven archetypes — each a package of horn family × driver complement × wall assignment × port scheme × crossover region, and each a fixed point of the constraint chain. Next UI evolution: replace open-ended options with an ARCHETYPE picker (each backed by a fixture): (1) Unity/CoSyne class — wood rect, small CD, 4×2″ open-back mids + tubes at the seams, 2 woofers straddle×2 on the wide walls [F1 GREEN]; (2) Coax 2-way MEH (Hinson/JMOD) — coax CD+mid in one, woofers only in horn; (3) Big-square quadrant (xoc1/reference big-square) — 4 woofers on 45° chamfer panels, square mouth [F3, needs octagon geometry]; (4) Molded/knuckle (SAWMOD/Solana) — sides entry, printed/molded [needs knuckles]; (5) Side-entry wide-format [F2, proportion-bound]. Customization lives INSIDE an archetype (scale, coverage, same-class driver swaps); crossing archetypes is where every infeasibility this session came from — the solver already names those. Implementation: archetype = named S-bundle (the fixtures’ specs) surfaced as the first UI choice; advanced remains for envelope-pushing.

> **BUILD 36 — SESSION CLOSE, GOLDEN FIXTURE GREEN.** Marwan’s methodology ask (“model the references, test the engine against them”) is now the tool’s test spine: meh_fixtures.js (in outputs) encodes his reference images as executable specs. F1 Unity/CoSyne class: **6/6 PASS** — first full clean reproduction of the reference through the complete engine. The stubborn “intersection” was probe-named: woofer rim point 2.6 mm inside the diagonal mid’s frame puck at (1.59,1.64,1.77)″ — solid-prim idealization of thin frame lips; resolved as a −3 mm LABELED tolerance (probe-verified, cited in the check row). DEF dwall='topbot' (fixture-proven: straddle ports fit the wide walls, not the sides, at CoSyne proportions; sides remains selectable — the side-canon references are bigger horns). F2 documents the sides proportionality constraint honestly (ports row); F3 documents the reference chamfer-quadrant dialect as the open feature gap. Next session: suite rebuild (all deltas), extend fixtures (F2 proportions, F3 octagon), drivers-first designer. Livetest 13/13 throughout.

> **BUILD 35 (deployed) — DRIVER WALLS = SIDES (corpus canon: reference big-square, Marwan’s reference set; the the Waslo calc sheet “#2 panels” never meant top/bottom — it means whichever pair you widen). Implemented as a virtual-frame transform: driverLayout computes in a w/h-swapped frame and swaps y↔z on output; containment rows and the first-flare slope are axis-aware; S.dwall ('sides' default | 'topbot') in advanced. Verify the A/B flows and render state — if “Mid ring vs woofer ring” persists on sides too, run the MOVE-#1 probe (which prim/point pair) before ANY fix.**
> **NEXT MAJOR FEATURE (Marwan’s ask, committed): DRIVERS-FIRST DESIGNER — user picks CD+mids+woofers; the tool derives the horn: (1) coverage+fc → mouth/angles as today; (2) driver-wall dimension floor: woofer frame+border at its first-flare-capped position sets a MINIMUM mouth height (sides) — iterate mouthH up until the woofer fits; (3) mid floor → XO ceiling vs CD reach as build 30; (4) report the derived horn + XOs, with every binding named. All pieces exist — it is an outer loop over mouth dims around the existing evaluate.**

> **BUILD 34 (FINAL, deployed 130,728) — MARWAN’S TOPOLOGY CORRECTION ENCODED.** His reference render settled it: the classic quadrant-square photo is TWO woofers × TWO ports each, not four woofers — the build-21 misreading that seeded nW=4. Now canonical on rect/sup: DEF nW=2, one woofer per #2 wall, centered, plW='straddle', npW=2 (4 holes = 2×2); mids default plM='diagonal' (corner seams around the CD, per the render — azimuthal interleave by construction). RESIDUAL: “Mid ring vs woofer ring” still FAILS (6 reds) on both reference flows even in this topology — auto-edge escalated, joint pass pushed L12 to 2.4. **MOVE #1 NEXT SESSION (after suite): direct probe of that pair — dump which prim/point pair yields the minimum (kind, prim r/h2, coordinates) for DEF; hand-estimates say frames ≈+7 mm and tubes clear, so suspect: the tube prim vs woofer MOTOR core, a stale joint-pass state, or a prims-construction offset. Fix what the probe shows; do NOT weaken the metric.** Suite deltas now include: nW=2 default breaks ‘12 port cuts (4 mid + 4×2 woof)’ expectations and hornresp sums — rebuild per §5 with all documented deltas.

> **BUILD 33 — FINAL SESSION STATE, READ FIRST (deployed meh_studio.html = 131,118 bytes).**
> Builds 27–33 happened after the updates below and their amend-chain partially no-op’d — trust THIS block + the session log over the per-build blocks. Final state: signed point-in-primitive collision metric (pointCylSigned over frame/motor/pad/tube solids — negative iff TRUE penetration; point clouds mathematically cannot go negative, which hid every interpenetration all session). The metric now truthfully shows the auto defaults RED: with mid and woofer laterals aligned, the mid mailing tubes thread into the woofer frame ring (“Mid ring vs woofer ring” FAIL on rect DEF + 3-way restore). Every reference photo resolves this by AZIMUTHAL INTERLEAVE — mids sit laterally between the woofers so tubes pass through the gaps. **Next session: (1) full suite run FIRST; (2) implement mid/woofer lateral interleave in the joint pass; expect DEF to clear. Do NOT weaken the metric to clear the reds — fix the layout.** Also final: geometry-first inverted pipeline (drivers→floors→XO ceilings, infeasible pairings named with remedies), velocity-sized ports, panel/board containment (frame on wood, pad on print), 6 mm physical pair floor (16 mm cosmetic tried/reverted), provenance rule encoded in engine + card, the Waslo calc sheet v5 XLS read directly (L12 default 1.5″ vs guidance 2–2.5″ tension displayed), B&C library + drawn models + visible fasteners, printable shell with splits. Livetest 13/13 on every deploy; the numeric suite has NOT run since build 23.
**Read the build-22 handoff first** (`meh_studio_handoff.md`, in Marwan's workset zip) — architecture, engine conventions, visual language, CoSyne verification, and Marwan's standards all still hold. This document layers builds 23–25 on top and supersedes its §5/§6/§9. It also supersedes `meh_studio_handoff_addendum.md`.

---

**BUILD 26 UPDATE (final, deployed 123,709 bytes)** — §3's open issue is RESOLVED and was two real engine gaps plus real physics, exposed by Marwan's placement screenshots: (1) ring-spacing checked only consecutive list entries, missing same-wall pairs (now all-pairs); (2) no geometric panel floor — ports could cross the corner creases (now a fit row + the tap walks OUT to where the wall holds the port, never past the first-flare cap); (3) no automatic port/driver decoupling (now: applyAutos escalation ladder — lateral both directions, then plM/plW→'edge' Waslo-style, driver outboard while the port keeps λ/4). Verified live: CoSyne default clean (worst pair honestly 1 mm warn); 4× bc5ndl mids auto-resolve via decoupling, 0 fails; the 2-way DE980+2×8PE21 lands geometrically clean at Lw 4.31" with the binding string stating the true tradeoff: "panel floor … OVER λ/4 — lower the crossover or shrink ports" (4.31"+1.7" path supports ≈560 Hz, not 650). Livetest 13/13; full suite still pending per §5 (add checks for: all-pairs, panel-floor row, the escalation ladder, and these three configs).

## 0. RELATIONSHIP STATE — read this before touching code
This was a hard session. Marwan escalated three times with screenshots, each one exposing a real engine defect (details in §3), plus one round where work I claimed wasn't visible to him. What rebuilt trust each time: taking the screenshot as data, tracing the fix to a literature source, and verifying by computation (pixel audits, engine probes) before presenting. What burned trust: presenting before suites ran, plumbing failures eating turns, and the sandbox silently recycling work. He asks for the deliverable repeatedly when frustrated — **deploy early and often; the outputs copy is the deliverable**. He is still waiting on one thing from his side: **B&C STEP files** (see §4).

## 1. CURRENT STATE & FILE LOCATIONS — inverted from build 22!
- **Source of truth: `/mnt/user-data/outputs/meh_studio.html` — build 25, 120,989 bytes.** The sandbox recycled mid-session and destroyed `/home/claude/hs` once; only outputs survived. Treat the working dir as disposable: `cp` the deployed file in, patch, verify, `cp` back out immediately. Byte counts are your checksums (117,483 → 120,313 → 120,475 → 120,989 this session).
- Working dir may contain **ghost artifacts** from interrupted turns (ab.js, probe*.js, meh_studio_b22.html, a stale meh_test.js). Interrupted assistant turns keep executing invisibly — audit any file you didn't just write (grep feature markers + run the engine sandbox) before building on it.
- Buildstamp: "build 25 · B&C driver library modeled from the mechanical drawings…".

## 2. WHAT BUILDS 23–25 ADDED (each rule → source)

**Build 23 — printable fabrication (the SCAD is now a complete part):**
- `shellSCAD` emits one printable body: revolve shell + per-driver **flat pads** (hull-skirted, sunk into the wall) + **heat-set insert pilots** on each bolt circle + **gasket relief** (hull of cone-clearance and port-cluster circles) + **surround recess** + ports (unchanged conventions: `port`/`portR`, frustum 1.5, Waslo 45°) + **CD plate** at the throat + **mouth flange** (8 holes) + **half/quarter splits** (`half=0|1|2|3|4`) with Ø3.2×16 seam-pin sockets on profile midline stations + flange. Boolean order: `diff(union(diff(union(body,pads), reliefs), body), bore() ∪ ports ∪ pilots ∪ cdholes ∪ flangeholes ∪ splitcut ∪ pins)`. **`bore()` re-cuts the horn interior as final authority** — verified by STL slicing (zero pad intrusion at 7 stations). Part is 180°-symmetric: print half=1 twice, or quarters 3 and 4 twice each (Ø18"+ horns: halves still span the full width; **quarters fit a 256 bed**).
- **CD pattern measured off Marwan's DH350 throat adapter STL: 2×Ø5.5 @ 53.0 mm c-c**, 1" bore; parametric with 3×57.15 BC / 4×101.6 BC alternates; `cd_az0=45` keeps bolts off both split planes. DE980TN/DCM420 presets (4×M6 Ø102) match the ≥1.2" default.
- Solana/SAWMOD/JMOD hardware canon distilled into `mountSpec` (§6) and the mounting card ("Print mounting — pads · inserts · fasteners" table + print-orientation prose: mouth-down, pins+epoxy, straddle-pads note).

**Build 24 — superellipse placement (his 2nd screenshot):**
- sup had inherited the round family's throat-ring placement → jumbled red cluster. Three fixes: (1) **sup routes through the wall-pair branch** (CoSyne/SAWMOD practice), seats projected onto the true superellipse via `azForLateral` (n=3 outline inversion); (2) **`cavityMargin` for sup is radial** vs the actual outline (was a box → correctly-seated drivers registered "inside" → false reds); (3) **pod-stand lift in `place()`**: on curved walls a flat frame seats on a faced pad — lift along the normal (12-azimuth bisection vs `cavityMargin ≥ wallT+3mm`) until the seat clears; `d.stand` recorded, pod drawn in 3-D. Build 25 capped it: **LMAX=35 mm; beyond that leave seated at wallT and let conformity FAIL honestly** (uncapped, oversize frames got lifted 90 mm along back-tilted normals into the CD — garbage geometry masquerading as fit fails).
- `S.latW` lateral fraction + applyAutos retreat loop (sup: 0.30→0.10) before the x walk-in.
- Verified: before/after pixel audit 14,315 red px → 0; driver mass 91% center → 26%.

**Build 25 — first-flare law + real B&C drivers (his 3rd/4th screenshots):**
- **`rollLimitX` generalized to every family**: drivers + pads live on the FIRST flare — before the mouth roll (round) or the 2nd-flare knee (rect/sup). rect/sup use the **vertical** slope (drivers ride #2 walls). Solver binding strings: "mouth roll (pad on straight flare)" / "panel fit (before the 2nd flare)". Placement clamps; fitCheck warn row "ring vs mouth roll / 2nd-flare knee". **Validation: CoSyne's published 4.4" woofer position computes as 4.29"** (10 mm pad margin) straight from the geometry. This fixed both the round-family crescents (build 22 bug, his 1st screenshot) and the sup drivers punching the mouth (my build-24 regression — the pod stand had let the solver park woofers past the knee at 7.85").
- **Past-mouth is a hard check**: rim+magRim sampled, >6 mm = FAIL + red.
- **B&C library** from his uploaded drawings — mechanical dims EXACT, acoustic values class estimates flagged "verify T/S": MIDS `bc5ndl`, `bc5nsm` (sealed), `bc8nsm` (sealed, hex frame); WOOFS `bc8pe`, `bc8nw`, `bc10mbx`, `bc12pe`, `bc12mh` (12MH36 PDF text layer corrupted — dims flagged verify); CDS `de980` (1.4", Ø131×54!), `dcm420` (2" coax mid body). Preset schema: `{od,dp,sd,cone,vtc,xm, bc,nb,bh,fl,bodyD,style:'slim'|'basket',sealed, fshape:'round'|'square'|'hex', p3:[[r_mm,z_mm]…], ribs}`. `sealed` → `S.vrcM=0`, no mailing tube (5NSM38/8NSM64 are the synergy darlings — sealed backs).
- **Drawing-traced 3-D models**: per-preset lathe profile `p3` (z behind the flange), square frames rotated 45° with ear radii (5NDL38/5NSM38 drawings), hexagonal frame (8NSM64), ribbed sealed cans (6 torus rings), basket+motor silhouettes for the rest. **Attachment is visible**: pad drawn at the REAL land diameter (`mountSpec.padD`) with **fastener heads at the real bolt circle/count**. Fallback: presets without `p3` use the generic parametric lathe.

## 3. THE OPEN ISSUE — resolve FIRST next session
The demo config **sup · topo `cw` (2-way MEH) · DE980TN · 2× 8PE21 · mouth 28"** still flags "Woofers vs CD body" FAIL (2 reds) at Lw=2.81" (λ/4 @ 650 Hz), even after the repro script toggled advanced + `plW='edge'`. My analysis says edge SHOULD clear it: offAx = 0.32·cone·cosψ ≈ 1.85" puts the frame center at ~4.66"; throat-side rim then lands at x≈+21 mm with ~19 mm CD clearance. The scripted output was **byte-identical before and after the plW change**, which smells like the select never took in the script (the probe doesn't echo S.plW — add that) rather than physics. Two-step plan:
1. Reproduce manually / fix the probe; if edge genuinely clears, the bug is in my repro not the engine.
2. Either way, implement **automatic port/driver decoupling for wall placements**: when the frame at the port position violates the wall or the CD, push the DRIVER center outboard along the wall (port stays at λ/4) until it fits — this is Waslo/forum canon already modeled for ring placements and is exactly how SAWMOD's knuckles and real builds resolve the big-frame/high-XO tension. The fit engine flagging Ø157/Ø225 frames at 650–950 Hz λ/4 positions is CORRECT physics, not a bug.

## 4. WAITING ON MARWAN
- **B&C STEP zips** (product page → Downloads → 3D model) for: DE980TN, DCM420, 5NDL38, 5NSM38, 8NSM64, 8PE21, 8NW51, 10MBX64, 12PE32, 12MH36. bc-speakers.com is NOT on the sandbox allowlist (verified; GitHub mirror search came up empty). When they arrive: decimate to ≤8k tris each (trimesh is installed via pip --break-system-packages), embed as compressed base64 per preset, replace the drawing-traced lathe bodies.
- Confirmed T/S values for the class-estimated Sd/Vtc/Xmax in the B&C presets; real 12MH36 dims.

## 5. VERIFICATION LEDGER — be honest about this state
- **GREEN this session on build 25**: livetest 13/13 (post driver-models); engine sandbox probes (knee caps all families, mountSpec real dims, SCAD pholes carrying 8×M6@210 + 4×M5@142); front pixel audit 0 red px; pastMouth 0.0 mm on the demo config; stand-cap behavior.
- **GREEN earlier epochs, not re-run**: full 96-check numeric suite (last full green was build 23-era 69/0 mid-run + hand-verified compiles); OpenSCAD compiles of full/half/quarter + bore-slice audit; sup before/after audits.
- **NEVER run**: full suite on builds 24–25. **The workspace meh_test.js is STALE** — rebuild from the pristine workset zip, then apply: (a) `-D '$fn=16'` on BOTH shq/shq0 execSync lines (raw file has plain quotes — transcript `\"` is JSON escaping, a trap that ate three patches); (b) the build-23 block (roll-limit, mouth-roll binding, rim-before-knee, warn-row, mount canon, print-shell counts 8/8/8, bore, splits, half-compile fn16); (c) solver check 7.65/entry-size → `4.29 ±0.15` + `/panel fit/`; (d) Lw=6.9 conformity check → clamp-to-knee + "2nd-flare knee" warn row; (e) preset checks (mountSpec 8PE21 → 8×M6 BC210; 5NDL38 → 4×M5 BC142); (f) NEW: sup wall placement (z-dominant normals, faces split, stands >12.5mm, radial cavityMargin ≈0 on surface, no-fails at wall canon with Lw 4.4); (g) probably: stand-cap test (oversize frame → conformity fail, NOT CD teleport); past-mouth magRim fail test.

## 6. NEW ENGINE SURFACE (additive to build-22 handoff §3)
- Exports added: `rollLimitX(S,st,kind)`, `mountSpec(S,kind)`.
- `mountSpec`: if preset dims present (`S.bc*/nb*/bh*`>0) → real path: bh≥6.2→**M6** (pilot Ø8.0×12.7, M6×20, padH 14); ≥4.9→**M5** (Ø6.4×9.5, M5×16, 12); else **M4** (Ø5.6×8.1, M4×12, 10); `bcD=bc·10`, `padD=max(od+12, bc·10+20)`, `real:true`. Else class fallback (od>15cm→M5×6 etc.).
- New S fields (all in localStorage `mehstudio_v3` — stale saved state remains a support question): `latW`, and per-section `bc,nb,bh,fl,bd,sty,p3,fsh,rbs` (+`sealM`). `d.stand` on layout entries.
- `azForLateral(st,x,y,face)` inverts the n=3 superellipse outline.
- SCAD test-regex invariants: `\) port[R]?\(`=12, `\) pad\(`=`\) prelief\(`=`\) pholes\(`=8 on the 4+4 cone-Ø18 state; `bore\(\);`, `half>=3`, `pin_d=3.2` present.

## 7. OPS LANDMINES (each cost real time this session)
1. **One heredoc body per bash command**; a missing body feeds the next block to the wrong interpreter.
2. **Transcript quotes are JSON-escaped** — `\"` in what you read is `"` in the file. Anchor structurally (element ids, regex) not by long literals; em-dashes and Unicode hyphens are similarly untrustworthy.
3. **Never `pkill/pgrep -f`** — the pattern matches the invoking shell's own command line (three self-kills, incl. via a later `nohup node meh_test.js` in the same command).
4. **Interrupted turns keep executing** → ghost edits; **sandboxes recycle mid-turn** → deploy after every verified increment; patches must be deterministic replayable string surgery.
5. Image viewer is intermittent (worked, then blanked twice). The quantitative protocol from the build-22 handoff stands: toDataURL captures + pixel statistics (red-collision count, spatial distribution) are the trusted eyes.
6. Suite compiles: run once, tee, background with `nohup … &` and poll; `$fn=16` for the heavy shell compiles.

## 8. GAPS RANKED
1. §3 open issue: auto port/driver decoupling on walls + settle the 2-way B&C config.
2. Suite rebuild + full green run on build 25 (§5).
3. Embed real STEP meshes when Marwan uploads (§4).
4. Angled/tilted pods (drivers aimed at the throat — the flower builds; still the unbuilt dialect; pod-stand lift is the halfway point).
5. SAWMOD knuckle geometry (S2 contour features) — would resolve the big-frame tension natively.
6. Response-preview losses; Hornresp record export; petal ports (unchanged from build-22 list).
7. Remind Marwan: journal entry + repo push (his desk ritual).

## 9. QUICK-START
```bash
mkdir -p /home/claude/hs && cd /home/claude/hs
cp /mnt/user-data/outputs/meh_studio.html meh_studio_v4.html    # deployed copy IS the source of truth
wc -c meh_studio_v4.html                                        # expect 120989
npm i three@0.128.0 puppeteer-core @sparticuz/chromium          # if node_modules missing
apt-get install -y openscad xvfb                                # compiles + PNG renders (xvfb-run)
unzip -o /mnt/user-data/uploads/meh_studio_workset.zip          # pristine meh_test.js base (if re-uploaded)
# engine smoke (no deps): extract the MEH IIFE, stub DOM, check solveTap/mountSpec — pattern throughout the session log
# first moves: §3 open issue → §5 suite rebuild → run both suites → deploy ritual (cp + present_files + bump stamp)
```
